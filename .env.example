## ENCRYPTION SETUP (Optional but recommended for production)
# 1. Generate a new key: npm run generate-key
# 2. Store the key securely: ENCRYPTION_KEY=<key-from-step-1>
# 3. Encrypt sensitive values: npm run encrypt -- "your-secret-value"
# 4. Replace plain values below with encrypted ones

# Encryption key (remove comment to enable encryption)
# ENCRYPTION_KEY="your-256-bit-hex-key-here"

## AniList OAuth Credentials
# Get from: https://anilist.co/settings/developer
# Plain: CLIENT_ID="..."
# Encrypted: ENCRYPTED_CLIENT_ID={"iv":"...","encryptedData":"...","authTag":"..."}
CLIENT_ID="get the id from here https://anilist.co/settings/developer"

# Plain: CLIENT_SECRET="..."
# Encrypted: ENCRYPTED_CLIENT_SECRET={"iv":"...","encryptedData":"...","authTag":"..."}
CLIENT_SECRET="get the secret from here https://anilist.co/settings/developer"

GRAPHQL_ENDPOINT="https://graphql.anilist.co"

## NextAuth Configuration
# Generate with: openssl rand -base64 32
# Plain: NEXTAUTH_SECRET='...'
# Encrypted: ENCRYPTED_NEXTAUTH_SECRET={"iv":"...","encryptedData":"...","authTag":"..."}
NEXTAUTH_SECRET='run this cmd in your bash terminal (openssl rand -base64 32) with no bracket, and paste it here'

NEXTAUTH_URL="for development use http://localhost:3000/ and for production use your domain url"

DISQUS_SHORTNAME='put your disqus shortname here (optional)'
# ADMIN_USERNAME="Your AniList account username"

## Database Configuration (SENSITIVE - Encrypt in production!)
# Use any postgresql db, or supabase for free options
# Plain: DATABASE_URL="postgresql://user:password@host:6543/postgres?pgbouncer=true"
# Encrypted: ENCRYPTED_DATABASE_URL={"iv":"...","encryptedData":"...","authTag":"..."}
DATABASE_URL="postgresql://user:password@host:6543/postgres?pgbouncer=true"

# Plain: DIRECT_URL="postgresql://user:password@host:5432/postgres"
# Encrypted: ENCRYPTED_DIRECT_URL={"iv":"...","encryptedData":"...","authTag":"..."}
DIRECT_URL="postgresql://user:password@host:5432/postgres"

## Optional Services
# If you don't want to use redis, just comment the REDIS_URL (press ctrl + / on windows or cmd + / on mac)
REDIS_URL="redis://user:password@host:port"